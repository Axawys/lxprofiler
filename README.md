# 🐧 Linux Psychological Profiler

Интерактивный профайлер на чистом **bash**, который «читает» вашу систему и
определяет психологический типаж пользователя. Скрипт анализирует дистрибутив,
окружение рабочего стола, железо, init-систему и установленные инструменты —
а затем распределяет очки между 11 архетипами и показывает результат в виде
интерактивного TUI.

## ⚡ Быстрый старт

Скачать, сделать исполняемым и запустить:

```bash
wget https://raw.githubusercontent.com/Axawys/lxprofiler/main/script.sh
chmod +x script.sh
./script.sh
```

> Запуск чужих скриптов из сети — на ваш страх и риск; при желании сначала
> просмотрите содержимое: `less script.sh`.

---

```
  Программист              100%  ████████████████████
  Системный администратор   76%  ███████████████░░░░░
▶ Анонимус                  49%  █████████░░░░░░░░░░░
  Геймер                    46%  █████████░░░░░░░░░░░
  DevOps-инженер            46%  █████████░░░░░░░░░░░
  ...
  ────────────────────────────────────────────
▶ Анонимус — 49%
  Тебя здесь не было. Tor, VPN и шифрование заметают следы за тобой.

  Что повлияло:
  GPG, VeraCrypt, cryptsetup, OpenVPN, rootless-контейнеры
```

## Возможности

- 🎯 **11 архетипов** с прозрачным обоснованием — видно, *что именно* повлияло на оценку
- 🖥 **Интерактивный TUI**: статичный список + зелёный маркер, который двигается по классам, и панель деталей внизу
- ⌨️ Навигация стрелками `↑/↓` и в стиле vim `j/k`, прыжки `g`/`G`, выход по `q`
- 📊 Нормализованные баллы и прогресс-бары
- 🔌 **Без зависимостей** — только `bash 4+`, coreutils и `tput`
- 🇷🇺 Поддержка отечественных дистрибутивов и софта
- 📄 Фолбэк на статический вывод при запуске в пайпе / не-TTY

## Архетипы

| Класс | Кого описывает |
|-------|----------------|
| **DevOps-инженер** | Контейнеры, k8s, Terraform, IaC — *cattle, не pets* |
| **Программист** | Языки, компиляторы, редакторы, IDE |
| **Системный администратор** | Живые серверы, демоны, бэкапы, мониторинг — *pets, не cattle* |
| **Минималист** | Мало процессов, лёгкие WM, dash, Alpine |
| **Бородатый юниксоид** | Был здесь до systemd: Emacs, suckless, alt-init, Gentoo |
| **Райсер** | Вылизанный рабочий стол, dotfiles, тема под скриншот |
| **Геймер** | Steam, Proton, Wine, библиотека игр |
| **Анонимус** | Tor, VPN, VeraCrypt, шифрование диска |
| **Хакер** | nmap, Metasploit, Wireshark, инструменты аудита |
| **Импортозамещённый** | Astra/RED OS/ALT/ROSA, Яндекс.Браузер, nginx, PostgreSQL |
| **Свидетель свежего ПО** | Wayland, Rust, rolling-release, атомарные обновления |

## Что анализируется

- **Дистрибутив** (`/etc/os-release`) — Arch, Fedora, Debian, NixOS, Gentoo,
  Kali, Astra, RED OS, ALT, ROSA и десятки других
- **DE / WM** — KDE, GNOME, XFCE, Hyprland, sway, i3, niri, dwm, qtile, xmonad…
- **Тип сессии** — Wayland / X11 / TTY
- **Железо** — объём RAM, число ядер CPU
- **Init-система** — systemd, runit, OpenRC, s6, dinit
- **Установленные инструменты** — контейнеры, языки, редакторы, БД, серверы,
  игровые лаунчеры, средства шифрования и аудита, пакетные менеджеры
- **Конфигурация пользователя** — shell, терминал, dotfiles, шифрование диска

## Запуск

```bash
chmod +x script.sh
./script.sh
```

Для статического вывода (например, чтобы сохранить результат) используйте пайп:

```bash
./script.sh | cat
./script.sh | tee profile.txt
```

## Управление в интерактивном режиме

| Клавиша | Действие |
|---------|----------|
| `↑` / `k` | поднять маркер |
| `↓` / `j` | опустить маркер |
| `g` | к первому классу |
| `G` | к последнему классу |
| `q` | выход |

## Требования

- `bash` 4.0+
- стандартные coreutils (`awk`, `grep`, `sort`, `find`, `df`…)
- `tput` (пакет `ncurses`) — для интерактивного режима

## Как это работает

Каждый признак системы начисляет очки одному или нескольким классам с коротким
обоснованием. После сбора данных баллы нормализуются относительно лидера (0–100 %),
классы сортируются по убыванию, а в нижней панели показывается описание выбранного
класса и список сработавших критериев. Логика подсчёта прозрачна — её легко
расширять: достаточно добавить вызов `add <класс> <очки> "<причина>"`.

## Лицензия

GPLv2
